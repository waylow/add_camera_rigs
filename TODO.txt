TO DO:
-add the dof object
-add the comp guides back

-combine the ui like in Brian's version

-get the collections working properly
(make sure it checks if it already exists)

-add preferences so the user can set their own naming convention (for collections and widgets)
